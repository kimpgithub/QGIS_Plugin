__pycache__/
.ipynb_checkpoints/
*.pyc
*.pyo
*.egg-info/
.eggs/
dist/
build/
*.so
.DS_Store
Thumbs.db

# --- server/ (HQ stack) ---
# 시크릿: 실제 값은 /srv/gis/compose/.env 에만, repo 에는 .env.example 만.
server/compose/.env
server/compose/*.env
server/compose/.web-credentials.txt
# 담당자 계정 평문 비밀번호 — git 보관 금지. 서버에 수동 복사(scp) 후 시드.
server/scripts/web_accounts.csv
# 런타임 데이터 (호스트 볼륨)
server/pgdata/
server/minio/
# 운영팀 반입 SHP 원본 (대용량/원본 데이터)
server/_admin_src/
server/**/*.shp
server/**/*.shx
server/**/*.dbf
server/**/*.prj
server/**/*.cpg
